PyYAML>=6.0
Jinja2>=3.0
# Add other dependencies here as needed, e.g.:
# openai
# anthropic
# google-generativeai
# Ruff (for linting, though usually installed globally or as a dev dependency)
# Black (for formatting, similar to Ruff)
httpx>=0.20 # For asynchronous HTTP requests
python-dotenv 